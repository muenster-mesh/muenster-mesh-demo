services:
  site:
    build: .
    ports:
      - "4000:4000" # site → http://localhost:4000/muenstermesh/
      - "35729:35729" # livereload
    volumes:
      - .:/site # live-edit source files from the host
